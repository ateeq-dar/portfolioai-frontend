const Footer = () => {
  return (
    <footer className="text-center py-16 text-gray-500 text-sm">
      © {new Date().getFullYear()} Ateequr Rahmaan — AI Portfolio
    </footer>
  );
};

export default Footer;